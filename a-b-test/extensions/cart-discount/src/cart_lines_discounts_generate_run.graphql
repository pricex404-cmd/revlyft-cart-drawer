query CartInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        ... on ProductVariant {
          id
          title
        }
      }
      cost {
        subtotalAmount {
          amount
        }
      }
    }
    # Individual cart attributes for revlyft IDs
    userIpAttribute: attribute(key: "revlyft_user_ip") {
      value
    }
    hashValueAttribute: attribute(key: "revlyft_hash_value") {
      value
    }
    testTargetingAttribute: attribute(key: "revlyft_tests_targeting_data") {
      value
    }
    testTimerAttribute: attribute(key: "revlyft_tests_start_time_data") {
      value
    }
    # Current time from frontend
    currentTimeAttribute: attribute(key: "revlyft_current_time") {
      value
    }
  }
  discount {
    discountClasses
    metafield(namespace: "$app:cart-discount", key: "function-configuration") {
      value
    }
  }
}
